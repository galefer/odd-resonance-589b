---
import gravetaleLogo from '../assets/gravetale_logo.png';
---
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>GRAVETALE Studios™</title>
    <style>
      body {
        background-color: #0b0b0b;
        color: #f0f0f0;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        margin: 0;
        padding: 0;
        overflow: hidden;
      }

      .container {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
        position: relative;
        text-align: center;
        z-index: 2;
      }

      img {
        width: 200px;
        margin-bottom: 1rem;
        filter: drop-shadow(0 0 15px rgba(255, 0, 0, 0.2));
      }

      h1 {
        font-size: 3rem;
        letter-spacing: 2px;
        margin: 0.5rem 0;
      }

      p {
        font-size: 1.2rem;
        max-width: 700px;
        opacity: 0.85;
        padding: 0 1rem;
      }

      .button {
        margin-top: 2rem;
        background: #8b0000;
        color: white;
        padding: 0.75rem 1.5rem;
        border: none;
        font-size: 1rem;
        border-radius: 5px;
        text-decoration: none;
        transition: background 0.3s ease;
      }

      .button:hover {
        background: #a10000;
      }

      .fog {
        background: url('https://i.ibb.co/vHzwhWk/fog-transparent.png') repeat;
        background-size: cover;
        position: absolute;
        top: 0;
        left: 0;
        width: 300%;
        height: 300%;
        opacity: 0.04;
        animation: drift 180s linear infinite;
        pointer-events: none;
        z-index: 1;
      }

      @keyframes drift {
        0% { transform: translate(-10%, -5%) scale(1); }
        100% { transform: translate(10%, 5%) scale(1); }
      }
    </style>
  </head>
  <body>
    <div class="fog"></div>
    <div class="container">
      <img src={gravetaleLogo.src} alt="GRAVETALE Studios™ logo" />
      <h1>GRAVETALE Studios™</h1>
      <p>Experiences designed to crawl under your skin. Coming to you soon.</p>
      <a href="https://discord.gg/YOUR_INVITE_CODE" class="button" target="_blank">Join Our Discord</a>
    </div>
  </body>
</html>
